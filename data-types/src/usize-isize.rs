fn main() {
  // usize and isize are nicknames for another exsiting Rust integer type.
  // Their size depends on the architecture of the computer your program is running on.
  // In other words, it varies on whether your program is running on a 32-bit or 64-bit system.
  let x: usize = 10;
  let y: isize = -5;
}